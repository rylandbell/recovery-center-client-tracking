extends ./layouts/2-col

block content
  .row
    .col-xs-12
      .panel.panel-primary
        .panel-heading 
          .panel-title Client List
        .panel-body
          table.row-border.order-column#client-table
            thead
              tr
                th.col-xs-4 Name&nbsp;&nbsp;
                th.col-xs-3 Clinician&nbsp;&nbsp;
                th.col-xs-2 Start date&nbsp;&nbsp;
                th.col-xs-3 Time since last check-in&nbsp;&nbsp;
            tbody
              each client in clients
                tr(id = "client_#{client.id}" data-tooltip='true' title='Click a row to visit the client\'s page' data-placement='bottom')
                  td #{client.lastName}, #{client.firstName}
                  td -
                  if (client.startDate)
                    td= client.startDate
                  else
                    td N/A
                  td -
            #pager
          //- Search form, hidden for now
          //- form(action="/", method="get", role="form").form
          //-   .form-group
          //-     label(for='action-select').control-label Find a client:&nbsp;&nbsp;
          //-     input(type='text' size='45')
          //-     |&nbsp; &nbsp;
          //-     button.btn-primary
          //-       .i.glyphicon.glyphicon-search 
          //-       |  Search
          //- .alert.alert-success.text-center
          //-   h4
          //-     strong Click on a row to view a client's dashboard.
block sidebar
  .grey-box
    h3 Welcome to List View
    p This space might hold:
    ul
      li A welcome message
      li Information about the app
      li Instructions for using this page and navigating the app
      li ???
